this is third exercise here the form inputs and buttons are saperated to across different chils components.
Material UI and react Hooks are used here

-----------------------------------------------------------------------------------------
Adding the solution forth exercice.(swaping two dimensional array)
var arr=[[5,6],[9,7]]
console.log(arr[0].map(function (ele, c) { return arr.map(function (r) { return r[c]; }); }))//will give the required array (row->column,coloumn->row>
